//addEntry makes a new line with the to-do item, with functioning buttons
function addEntry(value) {
	var contactbutton = copy.querySelector(".contactbutton");
	contactbutton.addEventListener("click",function(){
		//want to include a function here that will ping an e-mail program and pull up a blank e-mail with abigail@abigail.science as the addressee//
		list.removeChild(copy);
	});
}

